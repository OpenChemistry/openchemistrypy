{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://openchemistry.org/params.schema.json",
  "title": "Code parameters schema",
  "description": "A description of the parameters that can be passed to a code.",

  "definitions": {
  },

  "type": "object",
  "properties": {
    "type": "object",
    "description": "The are the generic parameters that are pass by the caller.",
    "basis": {
      "type": "string",
      "description": "Basis set",
      "enum": ["3-21g", "4-31G"]
    },
    "functional": {
      "type": "string",
      "description": "The functional",
      "enum": ["b3lyp", "pbe0"]
    },
    "calculationType": {
      "type": "string",
      "description": "The calculation type",
      "enum": ["energy", "optimization", "vibrational"]
    },
    "additionalProperties": true
  },
  "required": [ ]
}
